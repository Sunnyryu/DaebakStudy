### Tensorflow 


```

데이터를 가지고 학습을 시킴 => 트레이닝
(해당 데이터는 트레이딩 데이터!) 

regression 모델이 데이터를 학습하여 모델을 만듬!  (어떤 학생이 무엇을 하고 어떻게 나올까 하고 물어보면 regression 데이터를 보고 대략 얼마 나오지 않을까 라고 예측을 해줌!) => 리니어 리그레션이라고 함!

linear한 모델이 맞을 거라는 가설을 세움 => linear regression! / 어떤 선이 데이터에 가장 잘맞는지 학습을 함! 
ex) H(x) = Wx+b 라는 식의 함수식이 나올꺼임! 

거리가 먼지 가까운지 측정하는 것을 => Cost function(Loss function 이라고 할 수 있음!) => H(x) -y로 차이를 구할 수 있지만 좋지는 않아서 (H(x) - y)^2을 하여 차이값이 음수든, 양수든 제곱하여 양수로 받아서 구할 수 있음 

데이터 값이 3개라면 

((H(x)-y))^2 + (H(x)-y))^2 + (H(x)-y))^2)/3 을하여 그래프와 실제 데이터들이 차이나는 값을 구할 수 있음!!

1부터 데이터 갯수만큼을 극한값으로 나타낸 것이 cost라고 할 수 있음
cost에서 W,b를 가장 작은 값을 구하는 것이 목표라고 할 수 있겠음!! 

```


